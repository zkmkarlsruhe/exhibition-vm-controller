# Plugins are custom per installation and artwork-specific — they (and their
# asset/data directories) belong in the artwork's own repo, NOT here.
*.py
*/

# Except the framework examples
!__init__.py
!*.example.py
!README.md
!.gitignore
